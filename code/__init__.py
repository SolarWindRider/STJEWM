"""ST-JEWM source code.

Subpackages:
    core:    shared infrastructure (CEM, encode, envs, viz, model API)
    data:    data loaders (one per env)
    eval:    canonical evaluation entry points
    train:   canonical trainers
    theory:  formal propositions + proofs
    scripts: thin CLI wrappers, one per (model x env) combo
"""
